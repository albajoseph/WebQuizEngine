# WebQuizEngine
A full-stack RESTful API built with Java and Spring Boot. Features include secure user registration (BCrypt), multi-choice quiz creation, and persistent storage via H2/JPA. Implements advanced Spring Security for author-only deletions and Spring Data for paginated browsing of quizzes and user completion history.
